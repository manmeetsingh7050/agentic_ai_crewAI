# Agentic AI demo dependencies (Python 3.10+ required for create_agent).
# create_agent() lives in LangChain 1.0+, so we pin the 1.x line.
langchain>=1.0
langgraph>=1.0
langchain-google-genai
langchain-tavily
streamlit
mcpadapt>=0.1.0
crewai>=1.0.0
crewai-tools>=1.0.0
mcp>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0